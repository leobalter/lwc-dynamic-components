{
    "private": true,
    "name": "lwc-playground",
    "version": "2.23.5",
    "description": "Playground project to experiment with LWC.",
    "scripts": {
        "dev": "rollup -c --watch",
        "build": "NODE_ENV=production rollup -c"
    },
    "devDependencies": {
        "@jmsjtu/lwc": "0.0.0-demo",
        "@jmsjtu/rollup-plugin": "0.0.0-demo",
        "@rollup/plugin-replace": "^4.0.0",
        "rollup": "^2.79.0",
        "rollup-plugin-livereload": "^2.0.5",
        "rollup-plugin-serve": "^2.0.1"
    },
    "nx": {
        "targets": {
            "build": {
                "outputs": [
                    "./dist"
                ]
            }
        }
    }
}
